function twoSum(nums, target) {
  // wee're gpoing to use two pointer sliding window
  // we're going to sort the array first
  // then we're going to have two pointers
  // one at the beginning and one at the end



}


twoSum();
